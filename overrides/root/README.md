# Overrides — root

Overrides for `@nucleify/root` (`root/src/…`).

Mirror paths under this folder:

- Original: `root/src/composables/useAuth.ts`
- Override: `overrides/root/src/composables/useAuth.ts`
