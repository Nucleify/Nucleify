---
description: TypeScript aliases and conventions for @nucleify/root
globs: root/**/*.{ts,tsx,vue}
alwaysApply: false
---

# TypeScript — root

Strict mode, ESNext, bundler resolution. Config: `root/.config/tsconfig.json`.

## Path aliases

| Alias | Resolves to |
|-------|-------------|
| `nucleify` | `root/src/nucleify.ts` |
| `modules` / `modules/*` | `shared_modules` / `shared_modules/*` |
| `nuc_api` | `shared_modules/nuc_api/supabase/api/server.ts` |
| `nuc_server` | `root/src/server/nuc_server.ts` |
| `~/*` | `root/src/*` |

```bash
pnpm typeslint   # tsc --noEmit
```
