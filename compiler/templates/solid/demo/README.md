# @nucleify/solid

Minimal Vite + Solid app for compiler emit (`.tsx` in `src/components/`).

```bash
make solid
# or:
pnpm compiler -- scaffold solid
pnpm compiler -- build --app=solid
cd solid/demo && pnpm install --ignore-workspace && pnpm run --ignore-workspace dev
```
