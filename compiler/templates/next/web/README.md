# @nucleify/next-web

Product shell for **web** under Next (`next/web/`). Scaffolded by tryb B:

```bash
make web TARGET=next
# or
pnpm compiler -- convert web --target=next
```

Portable emit lands in `src/components/`. Canonical Nuxt product remains top-level `web/`.
